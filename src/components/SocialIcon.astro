---
import { Icon } from 'astro-icon'

export interface Props {
	href: string;
	icon: string;
	width?: string;
	color?: string;
}

const { href, icon, width, color, background1, background2 } = Astro.props;

const _width = width ? width : "50px";
const _color = color ? color : "black";
---

<div class="icon-container">
	<a href={href}>
		<Icon 
			name={icon}
			width={_width}
			class="social-icon"
		/>
	</a>
</div>


<style define:vars={{color: _color, background1, background2}}>
	.social-icon {
		color: black;
		padding: 2px;
	}
	.icon-container {
		border: transparent;
	}
	.icon-container:hover{
		background: #485563;
		background: -webkit-linear-gradient(to right, #29323c, #485563);
		background: linear-gradient(to right, #29323c, #485563);
	}
	.icon-container:hover .social-icon {
		color: var(--color);
	}
</style>
